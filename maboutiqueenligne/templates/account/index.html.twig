{% extends 'base.html.twig' %}

{% block title %}Mon compte - Ma Boutique En Ligne{% endblock %}

{% block content %}
    <h1>Mon Compte</h1>
    <!-- Here we check if the user has an admin role. If so, we display the link to the backoffice -->
    {% if is_granted('ROLE_ADMIN') %}
        <small><a href="/admin">Vous êtes un administrateur, accéder au backoffice ?</a></small><br>
    {% endif %}
    Bienvenue dans votre compte {{ app.user.firstname }}.<br>
    C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles.
    <hr>
    - <a href="{{ path('account_order') }}">Mes commandes</a><br>
    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/pin-code.png') }}" alt="Modifier mon mot de passe"><br>
                <a href="{{ path('account_password') }}">Modifier mon mot de passe</a><br>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/home.png') }}" alt="Gérer mes adresses"><br>
                <a href="{{ path('account_address') }}">Gérer mes adresses</a><br>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/img/purchase.png') }}" alt="Mes commandes"><br>
                <a href="{{ path('account_order') }}">Mes commandes</a><br>
            </div>
        </div>
    </div>
    <hr>
{% endblock %}
